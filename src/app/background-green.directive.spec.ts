import { BackgroundGreenDirective } from './background-green.directive';

describe('BackgroundGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundGreenDirective();
    expect(directive).toBeTruthy();
  });
});
